# SecureChat
정보보호론 RSA + AES 기반 암호화 채팅 및 파일 전송 프로그램
Information Security Chat

------------------------------------------
[개요]
------------------------------------------
이 프로그램은 RSA 공개키 암호 방식을 이용해 AES 대칭키를 교환하고,
이후 AES 암호화를 이용하여 클라이언트와 서버 간
안전한 채팅 및 파일 전송을 수행하는 시스템입니다.

------------------------------------------
[구성 파일]
------------------------------------------
client_ui.py     - 클라이언트 GUI 프로그램 (푸른색 테마)
server_ui.py     - 서버 GUI 프로그램 (초록색 테마)
crypto_utils.py  - RSA/AES 암호화 관련 함수 모듈
key_utils.py     - RSA 키 생성 및 로드 기능
keys/            - 클라이언트/서버의 공개키, 개인키 저장 폴더
files/
 ├── test_files/  - 클라이언트 전송용 파일 저장 위치
 └── received/    - 서버 수신 및 복호화 결과 저장 위치

------------------------------------------
[실행 방법]
------------------------------------------
1️. 먼저 서버를 실행합니다.
   > python server_ui.py

   - 포트 5000에서 클라이언트의 연결을 대기합니다.
   - 연결이 이루어지면 자동으로 RSA 공개키 전송 및 AES 세션키 수신이 수행됩니다.

2️.  다음으로 클라이언트를 실행합니다.
   > python client_ui.py

   - IP 주소(기본값 127.0.0.1)와 포트(기본값 5000)를 입력 후 "Connect" 버튼 클릭
   - AES 세션키를 생성하여 서버 공개키로 암호화 후 전송합니다.
   - 연결 후 채팅 및 파일 전송이 가능합니다.

3️. 채팅 및 파일 전송 테스트
   - 메시지 입력 → Send 클릭 시 AES 암호화 후 전송
   - 📎 버튼 클릭 → test_files 폴더에서 파일 선택 → 전송 완료 시 서버의 received 폴더에 복호화 결과 저장

------------------------------------------
[개발 환경]
------------------------------------------
- Python 3.10 이상
- 추가 설치 패키지 없음 (tkinter, cryptography 내장 라이브러리 사용)

------------------------------------------
[참고]
------------------------------------------
- RSA (2048-bit) 공개키 기반 암호화
- AES (128-bit CBC) 대칭키 암호화
- PKCS7 Padding 적용
- GUI: tkinter (기본 내장 라이브러리)

------------------------------------------
감사합니다.
